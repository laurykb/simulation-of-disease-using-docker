apiVersion: 1
datasources:
  - name: Postgres (MediComTel)
    uid: pg_main
    type: grafana-postgresql-datasource
    access: proxy
    isDefault: true
    url: db:5432
    user: admin
    database: base_test
    jsonData:
      sslmode: disable
      postgresVersion: 1400
    secureJsonData:
      password: admin